// Оборачиваем код в функцию


function reusableFunction() {
    console.log("Hi World");
  }
  

// Вызываем функцию. Вместо того что бы писать весь код заново. 
  reusableFunction();
  reusableFunction();
  reusableFunction();